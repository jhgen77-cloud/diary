이미지 첨부 모달창 구현

# 페이지 구성

- image-attachment.png를 클릭 시, 이미지 첨부 모달창을 구현할 것
- 상단 헤더
- 좌측: ‘이미지 첨부’로 텍스트 로고 배치할 것 (‘시간을 붙잡다’ 구현 방식과 동일)
- 우측: 컴포넌트 화면 최소화,최대화,닫기 버튼을 배치할 것 (기존 구현 방식과 동일)
- 메인 섹션
- 상단에 좌측에서 우측으로 순차적으로 add-image.png, remove-selection.png,
  zoom-in.png, zoom-out.png icon을 사용하여 배치할 것
- icon은 C:\webdev\Diary\images를 사용할 것
- icon은 비율에 맞게 작게 조정할 것
- icon은 C:\webdev\Diary\images를 사용할 것
- icon은 비율에 맞게 작게 조정할 것
- 이미지를 드래그할 컴포넌트를 구현하고, 박스를 쳐서 구분할 것
- ‘이미지를 드래그해서 추가하세요.’ 텍스트를 박스 정중앙에 배치할 것
- add-image.png를 클릭하면, 이미지 첨부를 위해 폴더를 열 것
- remove-selection.png를 클릭하면, 첨부된 이미지를 제거할 것
- 첨부된 이미지 사이즈는 원본 대비 작게 축소해서 첨부되게 하고, 드래그해서 첨부가
  되면 바탕화면에 보여지게 할 것
- zoom-in.png를 클릭하면, 첨부된 이미지가 첨부된 사이즈 기준 최대 2단계로
  확대되게 할 것,
- zoom-out.png를 클릭하면, 첨부된 이미지가 첨부된 사이즈 기준 최소 2단계로
  축소되게 할 것
  단, 이미지가 최대 2단계까지 확대된 상태면, 4단계 축소
- 첨부된 이미지는 자동 저장될 것

# 추가 수정 요청 사항(1)

- 시간을 붙잡다 모달창은 그대로 두고, 이미지 첨부 모달창을 하나 더 띄워 줄 것
  - 이미지 첨부 모달창은 바탕화면 내에서 자유롭게 이동 가능하게 구현할 것
    - 바탕화면 밖으로 벗어나지 않게 이동 가능하게 구현할 것

# 추가 수정 요청 사항(2)

- 이미지 첨부는 최대 5개까지 가능할 것
- 첨부된 이미지 위치는 좌측에 배치할 것
- 첨부된 이미지가 좌측부터 순차적으로 우측으로 배치할 것

# 추가 수정 요청 사항(3)

- 이미지가 최대 2단계까지 확대된 상태면, 단계적으로 4단계 축소
- 첨부된 이미지의 테드리는 없앨 것
- 이미지 드래그 박스는 세로로 2배 키울 것
  - 드래그 박스 비율에 맞춰서 첨부된 이미지 사이즈도 좀 더 키울 것

# 추가 수정 요청 사항(4)

- 이미지가 최대 2단계까지 확대된 상태면, 클릭할 때마다 한단계씩 4단계까지 축소
- 첨부된 이미지의 테두리 없앨 것
- 이미지 드래그 박스는 세로로 시간을 붙잡다 모달창의 반으로 줄일 것

# 추가 수정 요청 사항(5)

- 첨부된 이미지 테두리 삭제할 것
- 이미지 드래그 박스는 현재 사이즈에서 4분의 3정도로 줄일 것

# 추가 수정 요청 사항(6)

- 이미지 드래그 박스는 첨부된 이미지 5개가 채워질만큼 가로 사이즈를 맞추고 세로는 좀 더 줄일 것
- 이미지가 첨부가 되면, image-attachment.png 일부를 색상이 파란색으로 바뀌게 구현할 것

# 추가 수정 요청 사항(7)

- 이미지가 중복 첨부가 되면, "중복된 이미지입니다!'라고 작은 경고창을 띄워 주고 중복 첨부되지 않게 처리할 것
- 이미지 첨부될 때, 제목란에 이미지 파일명 설정하지 말 것

# 추가 수정 요청 사항(8)

- 이미지 첨부 모달창이 닫혀도 시간을 붙잡다 창이 강제로 닫히는 것 아니면, 계속 저장된 상태로 둘 것

# 주의 사항

    - 컴포넌트 단위로 적절히 분할해서 구현할 것
    - 결과는 컴포넌트 단위로 보고할 것
    - 이미 만들어둔 컴포넌트를 최대한 활용할 것
